name: explore
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.2
  flutter: '>=3.47.2'

dependencies:
  forui: ^0.26.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  equatable: ^2.1.0
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  domain_models:
    path: ../../domain_models
  component_library:
    path: ../../component_library
  routes_repository:
    path: ../../routes_repository
  recording_service:
    path: ../../recording_service
  route_planning:
    path: ../../route_planning

dev_dependencies:
  fake_async: ^1.3.3
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
