name: route_list
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.2
  flutter: '>=3.47.2'

dependencies:
  flutter_bloc: ^9.1.1
  domain_models:
    path: ../../domain_models
  flutter:
    sdk: flutter
  routes_repository:
    path: ../../routes_repository


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
