name: route_planning
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.2

dependencies:
  domain_models:
    path: ../domain_models
  http: ^1.6.0
  uuid: ^4.5.3

dev_dependencies:
  test: ^1.24.0
  lints: ^6.1.0
