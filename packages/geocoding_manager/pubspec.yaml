name: geocoding_manager
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.2

# Add regular dependencies here.
dependencies:
  geocoding: ^5.0.0
  http: ^1.6.0
  meta: ^1.19.0
  osm_nominatim: ^4.0.1
  domain_models:
    path: ../domain_models
  sqlite_storage:
    path: ../sqlite_storage


dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
