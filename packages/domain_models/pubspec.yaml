name: domain_models
publish_to: none

environment:
  sdk: ^3.4.0

# Add regular dependencies here.
dependencies:
  equatable: ^2.0.3
  meta: ^1.10.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
