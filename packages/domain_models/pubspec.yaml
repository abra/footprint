name: domain_models
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.2

# Add regular dependencies here.
dependencies:
  equatable: ^2.1.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
