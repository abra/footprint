name: routes_repository
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.2

dependencies:
  domain_models:
    path: ../domain_models
  sqlite_storage:
    path: ../sqlite_storage

dev_dependencies:
  sqflite_common_ffi: ^2.4.2
  lints: ^6.1.0
  test: ^1.24.0
