name: routes_repository
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.2

dependencies:
  characters: ^1.4.1
  flutter:
    sdk: flutter
  image_picker: ^1.2.3
  path_provider: ^2.1.5
  path: ^1.9.1
  uuid: ^4.5.3
  domain_models:
    path: ../domain_models
  sqlite_storage:
    path: ../sqlite_storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite_common_ffi: ^2.4.2
  lints: ^6.1.0
  test: ^1.24.0
