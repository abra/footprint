# Photo Fixture

`route_photo.png` is an unmodified embedded image extracted from the user-supplied
`Map Screen _ Recording State _ 3.pdf` Figma export (image 18).
It is used only by visual tests and is not bundled with the application.
